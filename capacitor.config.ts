import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Personal Banca',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
